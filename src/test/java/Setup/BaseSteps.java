package Setup;


import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

import io.cucumber.java.After;
import io.cucumber.java.Before;

public class BaseSteps {
	/*
	 * public WebDriver driver;
	 * 
	 * // Set up WebDriver before all tests
	 * 
	 * @Before public void setUp() { driver = new ChromeDriver(); // Initialize
	 * WebDriver (you can configure it as per your setup)
	 * driver.manage().window().maximize(); driver.get("https://www.nobroker.in/");
	 * // System.out.println(driver);// Open the homepage }
	 * 
	 * // Tear down WebDriver after all tests
	 * 
	 * @After public void tearDown() { System.out.println(driver); if (driver !=
	 * null) { driver.quit(); // Close the browser and quit WebDriver } }
	 */

//	public WebDriver getDriver() {
//		return driver;
//	}
//
//	public void setDriver(WebDriver driver) {
//		this.driver = driver;
//	}

    // Getter to access the WebDriver in other classes (like the step definitions)
	
	
	 
}
